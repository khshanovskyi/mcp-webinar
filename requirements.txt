# Host (agent) + servers
openai==2.24.0
# fastmcp powers both the MCP servers (FastMCP + auth providers) and the
# client (transports, OAuth). mcp is the underlying SDK (we import mcp.types
# directly); fastmcp 3.0.2 is compatible with mcp 1.28.0.
fastmcp==3.0.2
mcp==1.28.0

# HTTP clients / servers
httpx==0.28.1
requests==2.32.5
uvicorn==0.49.0
starlette==1.3.1

pydantic==2.12.5
